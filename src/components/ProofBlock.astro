---
---

<div class="bg-surface border border-border rounded-lg p-6 my-8 overflow-x-auto fade-up">
  <div class="flex items-center gap-2 mb-4">
    <span class="w-2 h-2 rounded-full bg-accent pulse-dot"></span>
    <span class="font-mono text-xs text-accent uppercase tracking-wider">Proof Verified</span>
  </div>
  <pre class="text-sm leading-relaxed font-mono"><code><span class="text-green-400">theorem</span> <span class="text-text-primary">zlib_decompressSingle_compress</span>
    (<span class="text-blue-400">data</span> : <span class="text-blue-400">ByteArray</span>) (<span class="text-blue-400">level</span> : <span class="text-blue-400">UInt8</span>)
    (<span class="text-blue-400">hsize</span> : <span class="text-text-secondary">data.size < 1024 * 1024 * 1024</span>) :
    <span class="text-blue-400">ZlibDecode</span>.<span class="text-text-primary">decompressSingle</span>
      (<span class="text-blue-400">ZlibEncode</span>.<span class="text-text-primary">compress</span> data level) = .<span class="text-green-400">ok</span> data</code></pre>
  <p class="text-text-muted text-xs mt-4">
    AI agent converted production C code (zlib) to Lean with machine-checked proofs. Minimal human guidance. No specialized model.
  </p>
</div>
